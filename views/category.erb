<ul class="thumbnails">
<% @repos.each do |repo_slug, repo| %>
  <% if repo_slug.split('.')[1] == @cat %>
  <li class="span2">
      <div class="thumbnail">
          <div class="thumb">
            <% if repo.iceberg %>
            <a href="/<%= repo.web_path %>/"><img src="/<%= repo.web_path %>/thumbnail/latest/iceberg/<%= repo.iceberg[0] %>"></a>
            <% end %>
          </div>
        <a href="/<%= repo.web_path %>/"><%= repo.title %></a>
      </div>
  </li>
  <% end %>
<% end %>
</ul>