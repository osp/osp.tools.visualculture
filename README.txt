OSP Visual Culture Git Viewer
=============================

- - -

$ sudo gem install sinatra grit haml shotgun

Use shotgun to serve with auto reload,
specify git repo as argument

$ shotgun app.rb "/home/e/osp/osp.work.panik"

